# wttool 

A small worktree management tool for quickly adding new worktrees to your 
project. 

Done in the workmux style under the `../$REPO__worktrees/<branch-name>` effect. 

## Usage

```
go run github.com/alexbathome/wttool@latest add <branchname> --base <optional base branch>
```
